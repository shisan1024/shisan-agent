# agent-provider

Provider crate for the `rust-agent` workspace.

## Overview

This crate provides provider implementations and integrations used by the agent.

## Getting Started

Build and test this crate from the workspace root:

```bash
cargo build -p agent-provider
cargo test -p agent-provider
```
