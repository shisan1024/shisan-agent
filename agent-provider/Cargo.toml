[package]
name = "agent-provider"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = "0.13.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

tokio = { version = "1", features = ["time"] }
