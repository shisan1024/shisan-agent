# agent-core

Core library crate for the `rust-agent` workspace.

## Overview

This crate contains the foundational agent logic and shared types for the workspace.

## Getting Started

Build and test this crate from the workspace root:

```bash
cargo build -p agent-core
cargo test -p agent-core
```
