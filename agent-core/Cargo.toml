[package]
name = "agent-core"
version = "0.1.0"
edition = "2024"

[dependencies]
agent-provider = { path = "../agent-provider" }
dotenv = "0.15"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
